{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test": "lerna run test --scope={@walnut/common,@walnut/common}",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
